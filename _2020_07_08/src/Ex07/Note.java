package Ex07;

public class Note {
/*
 * static "클래스의 "또는 '공통적인'의 의미를 가지고 있다.
 * 인스턴스 변수는 하나의 클래스로부터 생성되었더라도 각기 다른 값을 유지하지만,
 * 클래스변수(static 멤버변수)는 인스턴스에 관계없이 같은 값을 갖는다.
 * 클래스가 메모리에 로드 될때 생성된다.
 * 
 */
}
